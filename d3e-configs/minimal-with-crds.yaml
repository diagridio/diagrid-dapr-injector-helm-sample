# Minimal D3E Configuration with CRDs
# This is the basic deployment method with cluster-wide permissions
# Use this for development and testing environments

# Global settings
global:
  tag: "1.15.6"
  mtls:
    enabled: true
  actors:
    enabled: true
  scheduler:
    enabled: true
  rbac:
    namespaced: true
    namespaces: ["d3e-sample"]
    injector:
      enabled: true
    createTokenReviewerRole: true
    createTokenReviewerRoleBinding: true

# D3E token (replace with your actual token)
diagrid:
  token: "YOUR_D3E_TOKEN_HERE"

# Dapr control plane components
dapr_operator:
  enabled: true

dapr_sidecar_injector:
  enabled: true

dapr_placement:
  mode: "kubernetes"

dapr_scheduler:
  mode: "kubernetes"

dapr_sentry:
  mode: "kubernetes"
  injectDaprSystemConfig: false

dapr_config:
  dapr_config_chart_included: true

namespace: "d3e-sample" 